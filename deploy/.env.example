# 访问地址，两种填法：
#   有域名：DOMAIN=signal.example.com（DNS A 记录指向 VPS，Caddy 自动申请 HTTPS）
#   无域名：DOMAIN=:80（HTTP 明文，浏览器直接访问 http://VPS的IP）
DOMAIN=:80

# ---（可选）Basic Auth，默认关闭 ---
# 开启方式：解开 Caddyfile 中的 basic_auth 注释块，并填写下面两项后重启 caddy
# hash 生成：docker run --rm caddy:2-alpine caddy hash-password --plaintext '你的密码'
# ⚠️ docker compose 会对 .env 里的 $ 做插值，hash 中每个 $ 要写成 $$
# BASIC_AUTH_USER=farmer
# BASIC_AUTH_HASH=$$2a$$14$$replace-with-your-own-hash
